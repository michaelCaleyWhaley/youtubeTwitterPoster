# youtubeTwitterPoster

This bot is designed to post a tweet to Twitter. The post will consist of a title and link to the lastest upload of a given youtube channel.

There are six values which must be set in order for the bot to work. The values are in the .env.example.

## Steps to run
- npm install
- create .env file with values filled matches .env.example
- npm run start